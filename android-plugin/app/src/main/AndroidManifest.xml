<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Package visibility for Android 11+ -->
    <queries>
        <package android:name="com.maxmpz.audioplayer" />
    </queries>

    <application
        android:name=".PowerampStartRadioApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PowerampStartRadio"
        tools:targetApi="35">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PowerampStartRadio"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Indexing Activity -->
        <activity
            android:name=".indexing.IndexingActivity"
            android:exported="false"
            android:theme="@style/Theme.PowerampStartRadio"
            android:windowSoftInputMode="adjustResize" />

        <!-- Radio Service -->
        <service
            android:name=".services.RadioService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-initiated Poweramp recommendation queue generation and verified queue delivery" />
        </service>

        <!-- Indexing Service -->
        <service
            android:name=".indexing.IndexingService"
            android:exported="false"
            android:foregroundServiceType="mediaProcessing" />

        <receiver
            android:name=".indexing.V2IndexingBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Home Screen Widget -->
        <receiver
            android:name=".widget.StartRadioWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/start_radio_widget_info" />
        </receiver>
        <receiver
            android:name=".widget.StartRadioWidgetActionReceiver"
            android:exported="false" />

        <!--
          Poweramp's documented explicit event contract requires an exported manifest receiver and
          exposes no sender permission. Runtime code authenticates the sender when Android exposes
          its identity; otherwise delivery is only a refresh hint and command input is re-read and
          verified against Poweramp's providers.
        -->
        <receiver
            android:name=".poweramp.PowerampReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.maxmpz.audioplayer.TRACK_CHANGED_EXPLICIT" />
                <action android:name="com.maxmpz.audioplayer.STATUS_CHANGED_EXPLICIT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
